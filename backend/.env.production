# These are REFERENCE ONLY — set real values in Railway dashboard
# Do NOT commit actual secrets

DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/food_delivery?schema=public
PORT=4000
FRONTEND_URL=https://your-app.vercel.app
NODE_ENV=production
